{
  "name": "my-fullstack-app",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "concurrently \"cd backend && npm run build\" \"cd frontend && npm run build\"",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "docker:clean": "docker-compose down -v",
    "docker:build": "docker-compose build",
    "docker:start": "docker-compose up",
    "docker:start-d": "docker-compose up -d",
    "docker:pull-model": "docker-compose exec ollama ollama pull llama2"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
